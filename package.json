{
  "name": "shattered-mirror",
  "private": true,
  "version": "1.0.0",
  "description": "A compassionate, trauma-informed blog for children of narcissistic parents",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "vite": "^6.0.0"
  }
}